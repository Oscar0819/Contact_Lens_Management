<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ContactLensManagement" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/splash_background_color</item> <!-- 바텀 내비 아이콘 색생도 묶여있음 -->
        <item name="colorPrimaryVariant">@color/splash_background_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="LensInfoToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@color/splash_background_color</item>
        <item name="android:textColorSecondary">@color/splash_background_color</item>
    </style>

    <!-- TextView theme. -->
    <style name="LensInfoTextViewStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">#4186EF</item>
        <item name="android:textSize">20sp</item>
    </style>
    <!-- EditText theme. -->
    <style name="LensInfoEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">24sp</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- splash theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>
</resources>